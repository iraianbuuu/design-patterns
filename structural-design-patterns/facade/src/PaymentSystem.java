/**
 * PaymentSystem class represents the payment subsystem.
 * 
 */
public class PaymentSystem {
    public void processPayment(User user, int amount) {
        System.out.println("Payment processed successfully for " + user.getName() + " with amount " + amount);
    }
}
